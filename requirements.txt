# GenTwin-SG Pipeline — Python Dependencies
# Install with:  pip install -r requirements.txt

# Core data & ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tensorflow>=2.13          # or tensorflow-cpu if no GPU

# PDF scraping & parsing
requests>=2.31
pdfplumber>=0.10          # for extracting text from BPDB PDFs

# Feature engineering
holidays>=0.45            # Bangladesh holiday calendar

# Visualisation
matplotlib>=3.7
seaborn>=0.13

# Excel I/O
openpyxl>=3.1             # for .xlsx read/write

# Progress (optional but nice)
tqdm>=4.66

beautifulsoup4>=4.13     # for HTML parsing if needed
