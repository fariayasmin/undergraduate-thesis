# GenTwin-SG (Stages 0 -> K). The forecasting stage has its own requirements.
numpy>=1.24
pandas>=2.0
scipy>=1.11          # linprog/HiGHS; duals via res.ineqlin.marginals
shapely>=2.0         # Voronoi service-area clipping
matplotlib>=3.7
